import "../styles/globals.css";
import NexusAIButton from '../components/global/Nexus-AI/NexusAIButton';
export default function MyApp({ Component, pageProps }) {
return (
    <>
        <Component {...pageProps} />;
        <NexusAIButton />
    </>
)}