# Search Bar

<span style={{ color: 'grey', fontSize: 18, fontWeight: 400 }}>
  Displays a search bar with an optional animation.
</span>

<br></br>

import SearchBar from '../components/SearchBar/SearchBar';
import ComponentPreviewAndCode from '../components/ComponentPreviewAndCode/ComponentPreviewAndCode';
import CommandBlock from '../components/CommandBlock/CommandBlock'

<ComponentPreviewAndCode PreviewComponent={
    <SearchBar
        onChange={(value) => console.log(value)} // You can provide your onChange function here
        animated={true} // Set to true if you want animation, false otherwise
        IconColor="grey" // Change the icon color here
        className='rounded-full'
    />
}
Code={`
<SearchBar
    onChange={(value) => console.log(value)} 
    animated={true} 
    IconColor="grey" 
    className='rounded-full'
/>
`}
/>

## Installation

<CommandBlock command="npx react-nex add Search-Bar" />

## Example

### Search Bar Varients

<ComponentPreviewAndCode PreviewComponent={
    <div className='flex flex-col gap-6'>
        <SearchBar
            onChange={(value) => console.log(value)} // You can provide your onChange function here
            animated={true} // Set to true if you want animation, false otherwise
            IconColor="grey" // Change the icon color here
            className='rounded-sm'
        />
        <SearchBar
            onChange={(value) => console.log(value)} // You can provide your onChange function here
            animated={true} // Set to true if you want animation, false otherwise
            IconColor="grey" // Change the icon color here
            className='rounded-md'
        />
        <SearchBar
            onChange={(value) => console.log(value)} // You can provide your onChange function here
            animated={true} // Set to true if you want animation, false otherwise
            IconColor="grey" // Change the icon color here
            className='rounded-full'
        />
    </div>
}
Code={`
    <div className='flex flex-col gap-6'>
    <SearchBar
        onChange={(value) => console.log(value)} // You can provide your onChange function here
        animated={true} // Set to true if you want animation, false otherwise
        IconColor="grey" // Change the icon color here
        className='rounded-sm'
    />
    <SearchBar
        onChange={(value) => console.log(value)} // You can provide your onChange function here
        animated={true} // Set to true if you want animation, false otherwise
        IconColor="grey" // Change the icon color here
        className='rounded-md'
    />
    <SearchBar
        onChange={(value) => console.log(value)} // You can provide your onChange function here
        animated={true} // Set to true if you want animation, false otherwise
        IconColor="grey" // Change the icon color here
        className='rounded-full'
    />
</div>
`}
/>
