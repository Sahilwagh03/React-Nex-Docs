# Input

<span style={{ color: 'grey', fontSize: 18, fontWeight: 400 }}>
  Displays a check box or a component that looks like a check box.
</span>

<br></br>

import ComponentPreviewAndCode from '../components/ComponentPreviewAndCode/ComponentPreviewAndCode';
import CommandBlock from '../components/CommandBlock/CommandBlock'
import Input from "../components/Input/Input";
import InputWrapper from '../components/global/InputWrapper'


<ComponentPreviewAndCode 
PreviewComponent={
    <div className="w-[300px]">
        <InputWrapper />
    </div>
}
Code={`
import React, { useState } from 'react'
import Input from '../components-react-nex/Input/Input'

const InputExample = () => {
    const [value, setvalue] = useState('')
    return (
        <Input type="email" placeholder="j@example.com" value={value} onChange={setvalue}/>
    )
}

export default InputExample
`}
/>

## Installation

<CommandBlock command="npx react-nex add Input" />